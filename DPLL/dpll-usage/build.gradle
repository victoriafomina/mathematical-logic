plugins {
    id 'kotlin'
}

repositories {
    mavenCentral()
}

dependencies {
    implementation(project(":dpll-solver"))
    implementation(project(":dpll-core"))

    testImplementation platform('org.junit:junit-bom:5.9.1')
    testImplementation 'org.junit.jupiter:junit-jupiter'
}

test {
    useJUnitPlatform()
}